/**
 * Interactive authorization-code OAuth flow — see ./README.md.
 *
 * The harness lists this story as excluded via `package.json#example.excluded`;
 * this stub exists so the SKIP reason surfaces in the run-examples summary
 * alongside the other excluded stories. Run `./server.ts` and
 * `./simpleOAuthClient.ts` manually in two terminals.
 */
console.error('oauth/ is interactive — run server.ts + simpleOAuthClient.ts manually (see README.md)');
process.exit(1);
