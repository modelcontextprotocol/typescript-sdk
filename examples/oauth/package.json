{
    "name": "@mcp-examples/oauth",
    "private": true,
    "type": "module",
    "dependencies": {
        "@modelcontextprotocol/client": "workspace:*",
        "open": "^11.0.0"
    },
    "devDependencies": {
        "tsx": "catalog:devTools"
    },
    "example": {
        "excluded": "Interactive authorization-code OAuth flow: browser auth + callback server on :8090. Machine-to-machine client_credentials is ../oauth-client-credentials/; the readline playground is ../repl/."
    }
}
