{
    "name": "@mcp-examples/sse-polling",
    "private": true,
    "type": "module",
    "scripts": {
        "server": "tsx server.ts",
        "client": "tsx client.ts"
    },
    "dependencies": {
        "@modelcontextprotocol/client": "workspace:*",
        "@modelcontextprotocol/express": "workspace:*",
        "@modelcontextprotocol/node": "workspace:*",
        "@modelcontextprotocol/server": "workspace:*",
        "cors": "catalog:runtimeServerOnly",
        "express": "catalog:runtimeServerOnly"
    },
    "devDependencies": {
        "tsx": "catalog:devTools"
    },
    "example": {
        "transports": [
            "http"
        ],
        "excluded": "SEP-1699 SSE polling/resumption story is sessionful 2025 with server-initiated disconnect; the client's reconnect-then-replay flow is a long (~5s) wait that the harness doesn't bound well. Typecheck-only for now; revisit after the harness gets per-leg timeouts."
    }
}
