# schema-validators

Tool input/output schemas via Zod, ArkType and Valibot — any Standard-Schema-with-JSON library works. Also shows `outputSchema` → `structuredContent`.

```bash
pnpm tsx examples/schema-validators/client.ts
```
