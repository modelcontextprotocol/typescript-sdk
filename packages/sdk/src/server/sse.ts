// v1 compat: `@modelcontextprotocol/sdk/server/sse.js`
// The SSE server transport was removed in v2. Use Streamable HTTP instead.
// This stub keeps the module path resolvable but exports nothing.
export {};
