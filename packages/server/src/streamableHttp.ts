/**
 * Web Standards Streamable HTTP Server Transport
 *
 * This entry point provides the core StreamableHTTPServerTransport that works
 * with Web Standard APIs (Request, Response, ReadableStream).
 *
 * Usage:
 *   import { StreamableHTTPServerTransport } from '@modelcontextprotocol/server/streamableHttp';
 */
export * from './server/streamableHttp.js';
