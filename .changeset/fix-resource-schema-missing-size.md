---
'@modelcontextprotocol/sdk': patch
---

Add missing optional `size` field to `ResourceSchema` Zod schema, aligning with the MCP specification which defines `size` as an optional integer for resource contents.
